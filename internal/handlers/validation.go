package handlers

// Input validation constants for all handlers.
const (
	maxNameLength        = 200
	maxDescriptionLength = 1000
	maxIconLength        = 50
	maxCurrencyLength    = 3
	maxAmountValue       = 1e15
)
